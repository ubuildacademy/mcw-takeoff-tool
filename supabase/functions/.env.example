# Local development env for send-email-smtp
# Use with: supabase functions serve send-email-smtp --env-file .env

SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=mailer@yourdomain.com
SMTP_PASSWORD=your-mailbox-password
SMTP_FROM=mailer@yourdomain.com
